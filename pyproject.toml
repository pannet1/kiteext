[tool.poetry]
name = "kiteext"
version = "1.0.0"
description = "extn to kite"
authors = ["unknown"]
repository = "https://github.com/pannet1/kiteext"


[tool.poetry.dependencies]
kiteconnect = "^0.0.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
